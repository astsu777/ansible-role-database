---
# Default variables for this role

# Define the MariaDB root password
mysql_root_password: MyPassword

# Define if remote connections are allowed or not
db_remote_connections: deny

# If remote connections are authorized, define the IP address the server will listen to
db_server_bind_address: 1.1.1.1
